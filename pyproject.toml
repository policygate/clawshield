[project]
name = "clawshield"
version = "0.1.0"
description = "Security audit tool for OpenClaw deployments"
authors = [{name = "PolicyGate"}]
requires-python = ">=3.10"
dependencies = ["pyyaml"]

[project.scripts]
clawshield = "clawshield.__main__:main"
